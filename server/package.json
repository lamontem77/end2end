{
  "name": "recruiteros-slack-bot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Slack Events API backend for the RecruiterOS assistant. Untested in this environment — no Slack app credentials or shared database were available. See README.md before deploying.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^174.0.1",
    "node-cron": "^4.6.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
